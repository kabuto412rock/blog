services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/blog:cached
      - codex-home:/home/vscode/.codex
    command: sleep infinity

volumes:
  codex-home:
    external: true
